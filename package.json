{
    "name": "test-task",
    "private": true,
    "version": "0.1.0",
    "scripts": {
        "dev": "docker compose up -d postgres && npm --prefix apps/server run start:dev",
        "db:up": "docker compose up -d postgres",
        "db:down": "docker compose down",
        "stack:up": "docker compose up --build -d",
        "stack:down": "docker compose down -v"
    }
}